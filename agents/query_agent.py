def run_query_agent(work_directory_path: str, query_input: str) -> str:
    """
    Executes a graph-rag query within the specified working directory and returns the result as a string.

    Parameters:
    - work_directory_path (str): The absolute path to the directory where contains graph files or resources for processing the query.
    - query_input (str): A string representing the query question to be processed by the agent.

    Returns:
    - str: The answer of the query.
    """

    return ""